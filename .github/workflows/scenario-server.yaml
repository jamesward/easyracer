# Generated file. DO NOT EDIT.
'on':
  push:
    paths:
    - scenario-server/**
    - .github/workflows/scenario-server.yaml
  pull_request:
    paths:
    - scenario-server/**
    - .github/workflows/scenario-server.yaml
jobs:
  test:
    runs-on: ubuntu-24.04
    steps:
    - uses: actions/checkout@v5
    - uses: actions/setup-java@v5
      with:
        distribution: temurin
        java-version: '25'
        cache: sbt
    - uses: sbt/setup-sbt@v1
    - run: sbt test
      working-directory: scenario-server
  build-and-push-amd64:
    runs-on: ubuntu-24.04
    steps:
    - uses: actions/checkout@v5
    - uses: actions/setup-java@v5
      with:
        distribution: temurin
        java-version: '25'
        cache: sbt
    - uses: sbt/setup-sbt@v1
    - uses: docker/login-action@v3
      with:
        registry: ghcr.io
        username: ${{ github.actor }}
        password: ${{ secrets.GITHUB_TOKEN }}
    - env:
        DOCKER_IMAGE: ghcr.io/${{ github.repository }}
        DOCKER_TAG: ${{ github.sha }}-amd64
      run: sbt Docker/publishLocal
      working-directory: scenario-server
    - if: ${{ github.actor == github.repository_owner }}
      run: |-
        docker push ghcr.io/${{ github.repository }}:${{ github.sha }}-amd64
        if [ "${{ github.ref }}" = "refs/heads/main" ]; then
          docker tag ghcr.io/${{ github.repository }}:${{ github.sha }}-amd64 ghcr.io/${{ github.repository }}:latest-amd64
          docker push ghcr.io/${{ github.repository }}:latest-amd64
        fi
    needs: test
  build-and-push-aarch64:
    runs-on: ubuntu-24.04-arm
    steps:
    - uses: actions/checkout@v5
    - uses: actions/setup-java@v5
      with:
        distribution: temurin
        java-version: '25'
        cache: sbt
    - uses: sbt/setup-sbt@v1
    - uses: docker/login-action@v3
      with:
        registry: ghcr.io
        username: ${{ github.actor }}
        password: ${{ secrets.GITHUB_TOKEN }}
    - env:
        DOCKER_IMAGE: ghcr.io/${{ github.repository }}
        DOCKER_TAG: ${{ github.sha }}-aarch64
      run: sbt Docker/publishLocal
      working-directory: scenario-server
    - if: ${{ github.actor == github.repository_owner }}
      run: |-
        docker push ghcr.io/${{ github.repository }}:${{ github.sha }}-aarch64
        if [ "${{ github.ref }}" = "refs/heads/main" ]; then
          docker tag ghcr.io/${{ github.repository }}:${{ github.sha }}-aarch64 ghcr.io/${{ github.repository }}:latest-aarch64
          docker push ghcr.io/${{ github.repository }}:latest-aarch64
        fi
    needs: test
  create-manifests:
    runs-on: ubuntu-24.04
    if: ${{ github.actor == github.repository_owner && github.ref == 'refs/heads/main'}}
    steps:
    - uses: docker/login-action@v3
      with:
        registry: ghcr.io
        username: ${{ github.actor }}
        password: ${{ secrets.GITHUB_TOKEN }}
    - id: meta
      uses: docker/metadata-action@v5
      with:
        images: ghcr.io/${{ github.repository }}
        tags: type=sha
    - uses: docker/setup-buildx-action@v3
    - run: |-
        docker buildx imagetools create \
          -t ghcr.io/${{ github.repository }}:latest \
          ghcr.io/${{ github.repository }}:latest-amd64 ghcr.io/${{ github.repository }}:latest-aarch64
    needs:
    - build-and-push-amd64
    - build-and-push-aarch64
