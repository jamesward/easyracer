# Generated file. DO NOT EDIT.
'on':
  push:
    paths:
    - java-reactor/**
    - .github/workflows/java-reactor.yaml
  pull_request:
    paths:
    - java-reactor/**
    - .github/workflows/java-reactor.yaml
  workflow_run:
    workflows:
    - scenario-server
    types:
    - completed
jobs:
  test:
    runs-on: ubuntu-24.04
    if: ${{ github.event.workflow_run == null || github.event.workflow_run.conclusion == 'success' }}
    steps:
    - uses: actions/checkout@v5
    - uses: actions/setup-java@v5
      with:
        distribution: temurin
        java-version: '21'
        cache: gradle
    - uses: gradle/actions/setup-gradle@v5
    - run: ./gradlew test
      working-directory: java-reactor
    defaults:
      run:
        working-directory: java-reactor
